{
  "name": "loyaltyPlugin",
  "version": "1.0.0",
  "description": "Buildfire plugin to enable people to provide a reward and points system to their users.",
  "main": "index.js",
   "supportSite":"http://learn.appdocumentation.com/plugin-tutorials/loyalty-plugin-tutorial",
  "scripts": {
    "postinstall": "./node_modules/.bin/bower install",
    "test": "./node_modules/.bin/karma start --single-run --browsers PhantomJS",
    "build": "cd .. && rm -rf loyaltyPlugin.zip && zip -r loyaltyPlugin.zip loyaltyPlugin/ -x *node_modules/* *.git* *test/*"

  },
  "license": "ISC",
  "dependencies": {
    "bower": "^1.5.3",
    "karma-cli": "^0.1.0",
    "karma-coverage": "^0.5.0"
  },
  "devDependencies": {
    "chai": "^3.0.0",
    "jasmine-core": "^2.3.4",
    "karma": "^0.13.9",
    "karma-chai": "^0.1.0",
    "karma-chrome-launcher": "^0.2.0",
    "karma-coverage": "^0.5.1",
    "karma-jasmine": "^0.3.6",
    "karma-junit-reporter": "^0.3.3",
    "karma-mocha": "^0.2.0",
    "karma-phantomjs-launcher": "^0.2.1",
    "mocha": "^2.2.5",
    "phantomjs": "^1.9.17"
  }
}
